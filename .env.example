# Reference only: Python does not load this file automatically.
# Set these variables in your terminal as described in README.md.
OPENROUTER_API_KEY=replace-with-your-own-key
OPENROUTER_MODEL=openrouter/free
